import React from 'react';
import classes from './CurrentDialog.module.scss';

export const CurrentDialog = () => {
    return (
        <div className={classes.CurrentDialog}>

        </div>
    )
}